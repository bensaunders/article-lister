<h1>Articles</h1>
<% @articles.each do | article | %>
  <h2><%= article.details['title'] %></h2>
  <p>
    Likes: <%= article.likes %>
    <% if article.has_dynamic_like? %>
      👍
    <% else %>
    <%= link_to 'Like this article', action: 'like', id: article.id %>
    <% end %>
  </p>
<% end %>
